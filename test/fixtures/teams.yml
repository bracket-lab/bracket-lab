<%
Scenarios::Base::TEAM_NAMES.each_with_index do |name, i|
  starting_slot = i + 64
  seed = Team.seed_for_slot(starting_slot)
  region = i / 16
%>
team_<%= starting_slot %>:
  starting_slot: <%= starting_slot %>
  name: <%= name %>
  seed: <%= seed %>
  region: <%= region %>
<% end %>
